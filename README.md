const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];let DATA;
async function boot(){DATA=await(await fetch('assets/data/trip.json')).json();render();}
function render(){journey();days();places();count();mode();next();setInterval(count,1000);setInterval(next,60000)}
function journey(){$('#journeyStrip').innerHTML=DATA.journey.map(x=>`<div class="journey-chip"><span class="ico">${x.icon}</span><b>${x.label}</b><span>${x.time}</span><span>${x.duration}</span></div>`).join('')}
function days(){$('#dayGrid').innerHTML=DATA.days.map(d=>`<article class="day-card ${d.accent}" data-id="${d.id}" tabindex="0"><p class="kicker">${d.date}</p><h3>${d.title}</h3><ul>${d.summary.map(x=>`<li>${x}</li>`).join('')}</ul><span class="open-label">Open detailed day →</span></article>`).join('');$$('.day-card').forEach(c=>{c.onclick=()=>openDay(c.dataset.id);c.onkeydown=e=>{if(e.key==='Enter')openDay(c.dataset.id)}})}
function places(){$('#placeGrid').innerHTML=DATA.places.map(p=>`<a class="place-card" href="${p.url}" target="_blank"><small>${p.type}</small><h3>${p.name}</h3><p>${p.meta} →</p></a>`).join('')}
function openDay(id){const d=DATA.days.find(x=>x.id===id);$('#dialogDate').textContent=d.date;$('#dialogTitle').textContent=d.title;$('#dialogTimeline').innerHTML=d.timeline.map(x=>`<article class="timeline-item"><span class="timeline-time">${x.time}</span><h3>${x.icon} ${x.title}</h3><p>${x.text}</p>${x.actions?`<div class="timeline-actions">${x.actions.map(a=>`<a href="${a.url}" target="_blank">${a.label}</a>`).join('')}</div>`:''}</article>`).join('');$('#dayDialog').showModal()}
function count(){const t=new Date(DATA?.trip?.concert?.dateTime||'2026-07-26T20:30:00+02:00').getTime(),g=Math.max(0,t-Date.now());const v=[Math.floor(g/86400000),Math.floor(g%86400000/3600000),Math.floor(g%3600000/60000),Math.floor(g%60000/1000)];['daysNum','hoursNum','minutesNum','secondsNum'].forEach((id,i)=>$('#'+id).textContent=String(v[i]).padStart(2,'0'))}
function mode(){const n=Date.now();let t='Countdown mode',c='Prague is close. Bookings, journey and daily plans are ready.',p=0;if(n>=new Date('2026-07-25T05:00:00+01:00')&&n<new Date('2026-07-26T00:00:00+02:00')){t='Travel and arrival day';c='Doncaster → Heathrow → Prague → Café Savoy.';p=27}else if(n>=new Date('2026-07-26T00:00:00+02:00')&&n<new Date('2026-07-26T20:30:00+02:00')){t='Concert day';c='Explore Prague, eat properly, then travel to Letňany when the arrival plan is confirmed.';p=58}else if(n>=new Date('2026-07-26T20:30:00+02:00')&&n<new Date('2026-07-27T00:00:00+02:00')){t='Tonight: Pitbull';c='Mr Worldwide has reached Prague.';p=72}else if(n>=new Date('2026-07-27T00:00:00+02:00')){t='Castle and journey home';c='Checkout, Prague Castle, BA855 and the drive back.';p=84}$('#modeTitle').textContent=t;$('#modeCopy').textContent=c;$('#tripProgress').style.width=p+'%'}
function next(){const e=[['Leave Doncaster','2026-07-25T05:00:00+01:00'],['Maple Express','2026-07-25T08:00:00+01:00'],['BA852 departure','2026-07-25T10:15:00+01:00'],['Prague arrival','2026-07-25T13:15:00+02:00'],['Airbnb check-in','2026-07-25T15:00:00+02:00'],['Café Savoy','2026-07-25T18:00:00+02:00'],['Venue doors (not arrival target)','2026-07-26T15:00:00+02:00'],['Airbnb checkout','2026-07-27T11:00:00+02:00'],['BA855 departure','2026-07-27T17:45:00+02:00']].map(x=>({n:x[0],t:new Date(x[1])})).find(x=>x.t>Date.now());if(!e){$('#nextEvent').textContent='Trip complete';return}const m=Math.round((e.t-Date.now())/60000),h=m<60?`${m} min`:m<1440?`${Math.floor(m/60)}h ${m%60}m`:`${Math.floor(m/1440)}d ${Math.floor((m%1440)/60)}h`;$('#nextEvent').textContent=`${e.n} · ${h}`}
async function weather(){try{$('#weatherValue').textContent='Updating…';const j=await(await fetch('https://api.open-meteo.com/v1/forecast?latitude=50.0755&longitude=14.4378&current=temperature_2m&timezone=Europe%2FPrague')).json(),v=`${Math.round(j.current.temperature_2m)}°C in Prague`;$('#weatherValue').textContent=v;localStorage.weather=v}catch{$('#weatherValue').textContent=localStorage.weather||'Offline – no saved result'}}
async function fx(){try{$('#fxValue').textContent='Updating…';const j=await(await fetch('https://api.frankfurter.app/latest?from=GBP&to=CZK')).json(),v=`£1 = ${j.rates.CZK.toFixed(2)} Kč`;$('#fxValue').textContent=v;localStorage.fx=v}catch{$('#fxValue').textContent=localStorage.fx||'Offline – no saved result'}}
$('#weatherBtn').onclick=weather;$('#fxBtn').onclick=fx;$('#nextBtn').onclick=next;$('#dialogClose').onclick=()=>$('#dayDialog').close();$('#menuBtn').onclick=()=>$('#drawer').classList.add('open');$('#drawerClose').onclick=()=>$('#drawer').classList.remove('open');$$('.drawer a').forEach(a=>a.onclick=()=>$('#drawer').classList.remove('open'));if(localStorage.weather)$('#weatherValue').textContent=localStorage.weather;if(localStorage.fx)$('#fxValue').textContent=localStorage.fx;
let prompt;addEventListener('beforeinstallprompt',e=>{e.preventDefault();prompt=e;$('#installBtn').hidden=false});$('#installBtn').onclick=async()=>{if(prompt){prompt.prompt();await prompt.userChoice;prompt=null;$('#installBtn').hidden=true}};if('serviceWorker'in navigator)addEventListener('load',()=>navigator.serviceWorker.register('service-worker.js'));boot();
